import { useState, type FormEvent } from "react"; import { Paperclip, Send, Smile } from "lucide-react";
type Props = { channel: string; onSend: (message: string) => void };
export function MessageComposer({ channel, onSend }: Props) { const [message, setMessage] = useState(""); const send = () => { const trimmed = message.trim(); if (trimmed) { onSend(trimmed); setMessage(""); } }; const submit = (event: FormEvent) => { event.preventDefault(); send(); }; return <form onSubmit={submit} className="shrink-0 border-t border-white/[.06] bg-[#1b1b20] px-5 pb-5 pt-3"><div className="mx-auto max-w-4xl rounded-2xl border border-white/[.09] bg-[#131317] p-2 focus-within:border-stone-400"><textarea value={message} onChange={(event) => setMessage(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); send(); } }} rows={1} className="block min-h-10 w-full resize-none bg-transparent px-2 py-2 text-sm text-stone-100 outline-none placeholder:text-stone-600" placeholder={`Message #${channel}`} /><div className="flex items-center justify-between px-1"><div className="flex gap-1"><button type="button" className="grid size-8 place-items-center rounded-lg text-stone-500 hover:bg-white/[.07] hover:text-stone-100" aria-label="Attach a file"><Paperclip size={17} /></button><button type="button" className="grid size-8 place-items-center rounded-lg text-stone-500 hover:bg-white/[.07] hover:text-stone-100" aria-label="Add emoji"><Smile size={17} /></button></div><button disabled={!message.trim()} className="flex items-center gap-1.5 rounded-lg bg-stone-200 px-3 py-2 text-xs font-semibold text-stone-900 hover:bg-white disabled:bg-white/[.08] disabled:text-stone-600" aria-label="Send message"><Send size={15} /> Send</button></div></div></form>; }
